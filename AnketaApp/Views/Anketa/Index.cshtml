@model IEnumerable<AnketaApp.Models.Anketa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Naslov
        </th>
        <th>
            Broj pitanja
        </th>
        <th>
            Broj posjeta do sad
        </th>
        <th>
            Datum otvaranja
        </th>
        <th>
            Zadnja posjeta
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ankt_naslov)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ankt_brjpit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ankt_brjotvaranja)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ankt_datumotv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ankt_datposljotv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ankt_brj }) |
            @Html.ActionLink("Details", "Details", new { id=item.ankt_brj }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ankt_brj })
        </td>
    </tr>
}

</table>
